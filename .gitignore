.DS_Store
.playwright-mcp/
node_modules/
*.log
__pycache__/
# Служебные документы разработки — не публикуются (внутренняя кухня, цены, стратегия)
BRIEF-*.md
AUDIT-BRIEF.md
PLAN-*.md
UPGRADE-*.md
SPEC-OS.md
research/
TEST-*.md

# сгенерированные аудио-заготовки (не публикуем — используется живой агент)
audio/

# исходные PNG для images/ (тяжёлые, 17МБ) — в проде используется только webp;
# оригиналы регенерируются через tools/gen_images.py при необходимости
images/*/_orig/
